{
  "id": "kustomize",
  "name": "Kustomize  (via Github Releases)",
  "version": "1.1.1",
  "description": "Kustomize introduces a template-free way to customize application configuration that simplifies the use of off-the-shelf applications.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version of kustomize to install. Accepts versions with or without the 'v' prefix."
    }
  }
}